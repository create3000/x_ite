let i = 0;

const TraverseType = Object .freeze ({
   BBOX:      i ++,
   POINTER:   i ++,
   CAMERA:    i ++,
   PICKING:   i ++,
   COLLISION: i ++,
   SHADOW:    i ++,
   DISPLAY:   i ++,
});

export default TraverseType;
